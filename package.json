{
  "name": "greg-cli",
  "version": "0.3.1",
  "description": "Natural language to shell commands — powered by Apple Foundation Models",
  "type": "module",
  "bin": {
    "greg": "./dist/greg.mjs"
  },
  "scripts": {
    "build": "mkdir -p dist && bun build bin/greg.ts --outfile dist/_bundle.mjs --target node && printf '#!/usr/bin/env node\\n' | cat - dist/_bundle.mjs > dist/greg.mjs && rm dist/_bundle.mjs && chmod +x dist/greg.mjs && cp swift/afm-bridge.swift dist/",
    "test": "bun test",
    "prepublishOnly": "bun run build"
  },
  "files": [
    "dist/"
  ],
  "keywords": [
    "cli",
    "llm",
    "shell",
    "ai",
    "command-line",
    "apple-intelligence",
    "afm",
    "gemini"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
